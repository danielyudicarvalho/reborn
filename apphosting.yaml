# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Keep at least one instance warm and allow the service to scale
  # automatically to handle additional traffic.
  minInstances: 1
  maxInstances: 10
  # Allow each instance to handle multiple concurrent requests.
  concurrency: 80
